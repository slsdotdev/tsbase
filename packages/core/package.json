{
  "name": "@tsbase/core",
  "version": "1.0.0",
  "description": "Core utilities and types for the tsbase",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "lint": "eslint src --ext .ts",
    "dev": "vitest watch"
  }
}